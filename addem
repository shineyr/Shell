#!/bin/bash
# testing a multi-function script

name=`basename $0`

if [ $name = 'addem' ]
then
	total=$[ $1 + $2 ]
elif [ $name = 'multem' ]
then
	total=$[ $1 * $2 ]
fi

echo the calculated value is $total


